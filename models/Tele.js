import Model from './model.js';

export default class Tele extends Model {

  static table = "user.television";
  static primary = ["id"];

  // Additional methods or properties specific to the 'schema.tasks' table can be added here.
}